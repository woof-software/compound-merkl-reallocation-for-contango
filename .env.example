# JSON-RPC endpoints (Alchemy works out of the box)
RPC_MAINNET=
RPC_BASE=
RPC_ARBITRUM=
RPC_OPTIMISM=
RPC_POLYGON=

# RPC rate limiting (shared across all chains — one key, one compute-unit budget)
RPC_MAX_RPS=10
RPC_MAX_CONCURRENCY=5
RPC_TIMEOUT_MS=30000
